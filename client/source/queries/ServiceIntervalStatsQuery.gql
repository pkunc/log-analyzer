query ServiceIntervalStatsQuery($services: [String], $events: [String], $startDate: String, $endDate: String) {
  serviceIntervalStats(services: $services, events: $events, startDate: $startDate, endDate: $endDate) {
    service
    event
    occurrences
  }
}
